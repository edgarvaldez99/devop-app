version: 2
jobs:
    build:
        docker:
            image: node:10.7

steps:
    - checkout
    - setup_remote_docker
    - run:
        name: install_dependencies
        command: docker build -t devop-app -f ../Dockerfile
    - run:
        name: test
        command: - docker run devop-app npm test
